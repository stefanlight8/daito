[package]
name = "daito"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
daito-api = {workspace = true, optional = true}
daito-rpc = {workspace = true, optional = true}

[features]
api = ["dep:daito-api"]
rpc = ["dep:daito-rpc"]
